name: Plugins - CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: none
  id-token: none

jobs:
  ci:
    name: CI
    uses: grafana/plugin-ci-workflows/.github/workflows/ci.yml@91329403f85b22e31ac6cbe83910352b593fd090
    permissions:
      contents: read
      id-token: write
    with:
      plugin-version-suffix: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || '' }}
      run-playwright: true
      upload-playwright-artifacts: true
